extends layout
block content
  div.container.text-center
  - if(title === "Someone")
    a.btn.btn-lg.btn-primary(href="/users/signin") Sign in
  - else
    include navigation
  br
  img(src="/images/pting.png" height="300px" weight="300px")
  h1 Welcome to PTing

  a(href="/time") time
